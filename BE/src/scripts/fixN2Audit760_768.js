const fs=require('node:fs');const path=require('node:path');const file=path.resolve(__dirname,'../../file/n2_vocabulary.json');const d=JSON.parse(fs.readFileSync(file,'utf8'));const ws=d.units.flatMap(u=>u.parts.flatMap(p=>p.words));Object.assign(ws.find(w=>w.sourceNumber===760),{meaning:'Tỉnh giấc, mở mắt, nhận thức'});Object.assign(ws.find(w=>w.sourceNumber===768),{meaning:'Làm cho nguội, làm dịu đi, đàn áp, trấn áp'});const {computeCandidateHash}=require('../vocabulary/validateN2Vocabulary');d.verification.candidateHash=computeCandidateHash(d);fs.writeFileSync(file,`${JSON.stringify(d,null,2)}\n`,'utf8');console.log('Updated N2 meanings 760 and 768 from PDF pages 214-217.');
