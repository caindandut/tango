const fs=require('node:fs');const path=require('node:path');const file=path.resolve(__dirname,'../../file/n2_vocabulary.json');const d=JSON.parse(fs.readFileSync(file,'utf8'));const w=d.units.flatMap(u=>u.parts.flatMap(p=>p.words)).find(x=>x.sourceNumber===733);w.examples[0].segments=[{text:'波に揺られて、船が',reading:'なみにゆられて、ふねが',isUnderlined:false},{text:'上下さかさまになった',reading:'うえしたさかさまになった',isUnderlined:true},{text:'。',reading:'。',isUnderlined:false}];const {computeCandidateHash}=require('../vocabulary/validateN2Vocabulary');d.verification.candidateHash=computeCandidateHash(d);fs.writeFileSync(file,`${JSON.stringify(d,null,2)}\n`,'utf8');console.log('Scoped the first ひっくり返る example underline to the source phrase.');
