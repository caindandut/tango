const fs = require('node:fs'); const path = require('node:path'); const file = path.resolve(__dirname, '../../file/n2_vocabulary.json'); const d = JSON.parse(fs.readFileSync(file, 'utf8')); const w = d.units.flatMap(u=>u.parts.flatMap(p=>p.words)).find(x=>x.sourceNumber===618); const e = w.examples[2]; e.japanese='難しさを承知の上で、挑戦することにした。'; e.segments=[{text:'難しさを',reading:'',isUnderlined:false},{text:'承知',reading:'しょうち',isUnderlined:true},{text:'の上で、挑戦することにした。',reading:'',isUnderlined:false}]; const {computeCandidateHash}=require('../vocabulary/validateN2Vocabulary'); d.verification.candidateHash=computeCandidateHash(d); fs.writeFileSync(file,`${JSON.stringify(d,null,2)}\n`,'utf8'); console.log('Fixed word 618 example.');
