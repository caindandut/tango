const fs=require('node:fs');const path=require('node:path');const file=path.resolve(__dirname,'../../file/n2_vocabulary.json');const d=JSON.parse(fs.readFileSync(file,'utf8'));const ws=d.units.flatMap(u=>u.parts.flatMap(p=>p.words));Object.assign(ws.find(w=>w.sourceNumber===747),{kanji:'成る',hiragana:'なる',hanVietMeaning:'THÀNH',meaning:'Ra đời'});Object.assign(ws.find(w=>w.sourceNumber===753),{kanji:'尖る',hiragana:'とがる',hanVietMeaning:'TIÊM',meaning:'Nhọn, sắc'});const {computeCandidateHash}=require('../vocabulary/validateN2Vocabulary');d.verification.candidateHash=computeCandidateHash(d);fs.writeFileSync(file,`${JSON.stringify(d,null,2)}\n`,'utf8');console.log('Updated N2 words 747 and 753 from PDF pages 212-213.');
