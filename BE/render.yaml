services:
  - type: web
    name: tango-api
    runtime: node
    buildCommand: npm install && npx prisma generate && (npx prisma migrate resolve --rolled-back 20260810120000_add_vocabulary_examples || true) && npx prisma migrate deploy
    startCommand: node src/app.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tango-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false

databases:
  - name: tango-db
    plan: free
